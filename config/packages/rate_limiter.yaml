framework:
    rate_limiter:
        # API endpoint rate limiter: 100 requests per hour per IP
        api_limit:
            policy: 'sliding_window'
            limit: 100
            interval: '1 hour'
